// frequencies.js

class Frequencies {
  constructor(FrequencyID, HabitID, Frequency) {
    this.FrequencyID = FrequencyID;
    this.HabitID = HabitID;
    this.Frequency = Frequency;
  }
}

module.exports = Frequencies;
